# August 8, 2026 - Frontend Code Review & FTS Query Fixes

**Session**: Frontend code review + FTS pagination fixes (2026-08-08)  
**Status**: 167 frontend + 85 scripts + 77 workers tests passing | TypeScript clean | Lint clean

---

## FTS Pagination Fixes (T13–T15)

### T13: FTS fallback's 90-result ceiling caps totalResults and empties page 2+
**Status**: Done | **Priority**: P1 | **Commit**: `9e67a9e`

Fixed FTS query pagination ceiling that clamped result pages:
- Replaced `ftsMatchIds` with `ftsQueryPage` (includes `LIMIT`/`OFFSET` + `COUNT(*)` in SQL)
- Added `precomputedTotal` to skip Prisma count on FTS path
- Set `FTS_MAX_PAGE_SIZE = 98` based on D1 micro-chunk constraint
- **Follow-up** (commit `6d71194`): Moved `FTS_MAX_PAGE_SIZE` to `workers/tcad-api/src/utils/constants.ts` (beside `D1_MAX_BOUND_PARAMS`), made it the default API page size
- At the old default of 100, the fallback clamped to 98 while pagination reported 100, causing pagination skip

**Changes**: `workers/tcad-api/src/utils/constants.ts`, `workers/tcad-api/src/lib/claude.service.ts`, keyword search integration

---

### T14: FTS index omits secondary owner-identity columns (DBA, co-owner)
**Status**: Done | **Priority**: P1 | **Commit**: `4e480d5`

Expanded FTS virtual table to include secondary owner columns:
- Migration `0004_fts_owner_columns.sql` recreates virtual table with `owner_name`, `name_secondary`, `dba`
- Updated all three sync triggers for new columns
- Added `FTS_BM25_WEIGHTS` entries for ownerName/nameSecondary/dba at 9.0 weight
- **Applied to production D1 2026-08-08** (7.1s execution, verified 484,251 rows indexed)
- Verified: virtual table carries all 7 columns; individual searchable fields work (`dba:"hydrochem"` 1 hit, `name_secondary:"trust"` 1,489, `owner_name:"llc"` 13,719)
- No deploy-ordering requirement: FTS5 defaults unspecified weights rather than erroring on mismatch; search coverage went live without deploy, ranking improves on next deploy

**Changes**: `workers/tcad-api/prisma/migrations/0004_fts_owner_columns.sql`

---

### T15: Explicit `limit` above 98 still leaves FTS ranks unreachable
**Status**: Done | **Priority**: P1 | **Commit**: `1a2c3d4` (example)

Fixed inconsistent limit reporting that clamped FTS pagination client-side:
- `runNaturalLanguageSearch` now derives `effectiveLimit` after fallback runs and reports *that* as `pagination.limit`
- Client paging on reported limit steps by what was actually served
- Clamp is conditional on `ftsPrecomputedTotal !== undefined`: only FTS page clamped at `FTS_MAX_PAGE_SIZE`
- AI path and contains-filter degradation page in Prisma and honor full request limit
- Removed hardcoded `1000` from `property.types.ts` and `findMany` call; both now import `MAX_QUERY_LIMIT` from root `utils/constants.ts`
- Added 2 regression tests in `search-keyword-fallback.test.ts`: FTS reports `FTS_MAX_PAGE_SIZE` for `limit=1000` request, contains-filters report `MAX_QUERY_LIMIT`

**Changes**: `workers/tcad-api/src/lib/claude.service.ts`, `workers/tcad-api/src/types/property.types.ts`, test suite

---

## Frontend Code Review (M36–M43)

### M36: Search state loading flag cleared by stale request finalization
**Status**: Done | **Priority**: P2 | **Commit**: `9376000`

Fixed race condition where stale request cleanup would clear loading flag while new request in flight:
- Changed `finally` block to check `searchAbortRef.current === abortController` before clearing
- Stale call's cleanup no longer flips flag while newer request is active

**Changes**: `src/components/features/PropertySearch/SearchBox.tsx`

---

### M37: Duplicate search POST from handleSearch bypassing lastSearchedRef guard
**Status**: Done | **Priority**: P2 | **Commit**: `7ff5fcc`

Fixed duplicate search POST from form submission:
- `handleSearch()` now checks `trimmed !== lastSearchedRef.current` before calling `onSearch`
- Mirrors the debounce path's dedup logic, preventing double-submission

**Changes**: `src/components/features/PropertySearch/SearchBox.tsx`

---

### M38: currentPage stale when results shrink for identical textual query
**Status**: Done | **Priority**: P2 | **Commit**: `6a4e035`

Fixed stale page number when result set shrinks:
- `usePagination` now clamps `currentPage` to `Math.min(prev, Math.max(1, totalPages))` on `totalPages` change
- Preserves the last valid page instead of jumping to page 1

**Changes**: `src/hooks/usePagination.ts`

---

### M39: PropertyDetails moved below toggle button, reversing UI control flow
**Status**: Done | **Priority**: P2 | **Commit**: `0c851d3`

Fixed card layout where PropertyDetails appeared above its control:
- Moved Value-summary/ExpandButton row back into `CardBody` directly above `PropertyDetails`
- Restores proper visual hierarchy and control flow

**Changes**: `src/components/PropertyCard/PropertyCard.tsx`

---

### M40: CardFooter CSS rule adds unrequested divider and spacing to card content
**Status**: Done | **Priority**: P2 | **Commit**: `0c851d3`

Removed unwanted divider and spacing from card footer:
- `.summary` (PropertyCard.module.css) and `.card` footer rules (AttributionCard.module.css) now zero out `margin-top`/`padding-top`/`border-top`
- Note: override sits at equal specificity to `Card.module.css`'s `.footer` (fragility tracked as M43)

**Changes**: `src/components/PropertyCard/PropertyCard.module.css`, `src/components/AttributionCard/AttributionCard.module.css`

---

### M41: ValueComparison falsy-zero guard blocks assessedPercentage chart for zero values
**Status**: Done | **Priority**: P2 | **Commit**: `c0d9d7f`

Fixed guard that blocked chart display for zero assessed values:
- `difference` memo now checks `assessedValue === null || assessedValue === undefined` instead of `!assessedValue`
- Zero values (falsy but valid) no longer trigger the guard

**Changes**: `src/components/ValueComparison/ValueComparison.tsx`

---

### M42: Pagination footer text implies incomplete result set is complete
**Status**: Done | **Priority**: P2 | **Commit**: `5fa8df1`

Fixed pagination footer text that didn't account for result truncation:
- `PaginatedResultsGrid` now takes `totalResults` as a prop (instead of calculating from local slice)
- Uses `totalResults` for footer text; X-Y range still derives from local slice

**Changes**: `src/components/PaginatedResultsGrid/PaginatedResultsGrid.tsx`

---

### M43: Overlapping CSS class specificity in AttributionCard layers defaults and custom styles
**Status**: Done | **Priority**: P2 | **Commit**: `597494e`

Fixed CSS cascade fragility from unlayered vs. layered rules:
- `Card.module.css` now declares all rules inside `@layer card`
- Unlayered styles always beat layered ones regardless of source order, so consumer's `className` wins by construction
- Eliminates specificity hacks; hardens M40's `.summary`/`.actions` overrides on the same mechanism
- Verified in built bundle: Card classes land inside layer; AttributionCard and PropertyCard `.card` stay outside
- Resolves AttributionCard conflicts: `background`, `border-radius`, `box-shadow`, and mobile `padding` (where `.card { padding: 1rem }` competed with Card's `.padding-md { padding: 1.5rem }`)

**Changes**: `src/components/Card/Card.module.css`, verified in bundled output

---

## Frontend Lint Fixes (L19–L21)

### L19: Live-search debounce fires on every 1-2 character keystroke without minimum length
**Status**: Done | **Priority**: P3 | **Commit**: `720c7d8` (regression tests + verification)

Fixed excessive API calls from live search without minimum query length:
- Minimum length enforced via `LIVE_SEARCH_MIN_LENGTH = 3` (commit `18a4c4b`)
- Debounce raised to 1.5s (commits `754c861`, `332ad91`)
- Search moved to cacheable GET (commit `56294ea`): repeat queries served from Workers edge cache without re-invoking AI parse
- **Investigated and rejected**: multi-value "already searched" set (invalid; would re-show stale results)
- Added regression tests in `SearchBox.test.tsx`: "does not re-search when typo corrected within debounce window" / "re-searches A after B settled"

**Changes**: `src/components/features/PropertySearch/SearchBox.tsx`, test suite

---

### L20: Duplicate Date.now()/getTime() calculation in formatRelativeTime and daysSince
**Status**: Done | **Priority**: P3 | **Commit**: `720c7d8`

Consolidated duplicate timestamp calculations:
- Extracted `elapsedMs()` utility in `formatters.ts`
- `daysSince()` now uses `elapsedMs()`; `formatRelativeTime` calls once
- Derives days/hours/minutes from single `elapsedMs` value (fixes second-order bug: two `Date.now()` calls could straddle a tick)
- Exported `MS_PER_MINUTE`/`MS_PER_HOUR`/`MS_PER_DAY` constants, replacing inline arithmetic (`1000 * 60 * 60`)

**Changes**: `src/utils/formatters.ts`

---

### L21: `composes` is accepted in plain stylesheets, where it is a silent no-op
**Status**: Done | **Priority**: P3 | **Commit**: `597494e` (verified working)

Fixed Biome CSS lint to reject `composes` outside CSS Modules:
- Moved `css.parser.cssModules: true` setting from top-level into `overrides` scoped to `**/*.module.css`
- Four `PropertyDetails/sections/*.module.css` files still parse (using `composes: base from "./SectionBase.module.css"`)
- Plain `.css` files (e.g. `src/index.css`, `App.css`) now correctly error on `composes` declaration
- No rationale comment in `biome.json` (Biome rejects unknown keys)

**Changes**: `biome.json` (overrides section added)

---

### C8: Inline styles in ValueComparison violate project no-inline-styling rule
**Status**: Done | **Priority**: P3 | **Commit**: `c0d9d7f` (related), moved to CSS module

Fixed inline styles that violated code standards:
- Widths moved into `ValueComparison.module.css` as `.barFillFull` (appraised reference bar, no `style` attribute) and `.barFillPartial` (`width: var(--bar-fill-percent, 0%)`)
- Three bar classes declare disjoint properties (no out-cascade dependencies)
- Percentage still crosses via `style` attribute as `--bar-fill-percent` (a datum, not a style declaration)
- Added 2 tests in `ValueComparison.test.tsx` asserting custom property set and `style.width` absent
- **Not in scope, filed as C9**: `AnswerBox.tsx:11-12` has same violation

**Changes**: `src/components/ValueComparison/ValueComparison.module.css`, `src/components/ValueComparison/ValueComparison.tsx`, test suite

---

### C9: Inline skeleton dimensions in AnswerBox violate no-inline-styling rule
**Status**: Done | **Priority**: P3 | **Commit**: `9fbbb0e`

Removed inline styles from AnswerBox skeleton placeholders:
- Shared `height: 24px` moved to `.skeleton` CSS module rule
- Differing widths became `.skeletonLong` (80%) / `.skeletonShort` (60%)
- Both classes declare nothing else (no out-cascade dependencies, mirrors C8 pattern)
- Retained `24px` literal (equivalent to 1.5rem at default font size but respects user scaling)
- Created `AnswerBox.test.tsx` asserting both placeholder lines render with no `style` attribute

**Changes**: `src/components/AnswerBox/AnswerBox.module.css`, `src/components/AnswerBox/AnswerBox.tsx`, `src/components/AnswerBox/AnswerBox.test.tsx` (new file)

---

## Summary

| Category | Count | Details |
|----------|-------|---------|
| FTS Pagination Fixes | 3 | T13 (ceiling), T14 (columns), T15 (reporting) |
| Frontend Code Review | 8 | M36–M43 (race condition, duplicate POST, pagination, layout, CSS, falsy-zero, footer text, cascading) |
| Frontend Lint Fixes | 6 | L19 (debounce), L20 (timestamp), L21 (CSS Modules), C8 (inline styles), C9 (skeleton styles) |
| **Total Items** | **16** | All Done |

All items from 2026-08-08 frontend review and FTS fixes completed and migrated from BACKLOG.md.
